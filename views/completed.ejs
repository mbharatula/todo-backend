<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Completed Tasks</title>
    <link rel="stylesheet" href="/public/css/style.css">
    <link rel="icon" type="image/png" href="/public/images/favicon.png">
</head>
<body>
    <div class="container">
        <h1>Completed Tasks</h1>
        <div class="task-list">
            <% for(let task of completedTasks){ %>
                <div class="task-item">
                    <div class="task-content">
                        <div class="task-title"><%=task.title%></div>
                        <div class="task-desc"><%=task.desc%></div>
                    </div>
                    <div class="task-actions">
                        <form method="POST" action="/revert/<%=task.id%>">
                            <button type="submit" class="btn btn-secondary">Revert</button>
                        </form>
                        <form method="POST" action="/completed-tasks/delete/<%=task.id%>?_method=DELETE">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                </div>
            <% } %>
        </div>
        <a href="/" class="btn btn-link">Back to Active Tasks</a>
    </div>
</body>
</html>